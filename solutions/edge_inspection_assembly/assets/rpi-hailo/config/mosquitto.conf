# 仅用于本地部署与调试：匿名接入、只监听本机网络。
# 生产环境请换成带凭据的 broker（listener + password_file，或指向现有的企业 broker），
# 并把 config.json 的 mqtt.host 指过去。
listener 1883 0.0.0.0
allow_anonymous true
persistence false
log_dest stdout
