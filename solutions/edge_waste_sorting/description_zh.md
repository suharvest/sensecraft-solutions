# 投放点垃圾分类

触发拍一张，拿回这件东西是什么材质、该进中国生活垃圾四分类里的哪一档，
通过 MQTT 一条消息发出去。

## 这个方案做什么

一次触发——按钮、HTTP 调用，或画面里的移动——让设备拍一张图，把图里的物品
分到八个物料类别之一，由这个类别查表得到中国四分类，然后发一条 MQTT 消息，
带上类别、四分类、带置信度的 top3，以及所存图片的引用。图片字节不出设备；
payload 里只有路径或对象存储 URI。同时一个异步回调收到四分类结果，
翻盖、继电器或指示灯可以据此动作。

## 你会得到什么

- **一个头出两层答案。** 模型预测八个物料类别——paper、cardboard、glass、
  metal、plastic、textile、organic、residual。中国四分类（可回收物 / 厨余垃圾 /
  有害垃圾 / 其他垃圾）是它上面的一张查表，不是第二个头，所以各地口径变化
  是改表而不是重训。
- **触发即拍，不是视频流。** 按钮、HTTP 或移动侦测，800 ms 去抖；
  前一次未完成时到达的触发会被合并而不是排队。也有连续模式，限速运行，
  且要连续三帧 top-1 相同才发布。
- **契约是被检查的，不只是写在文档里。** 每条 payload 发布前都过一遍事件
  schema 校验，不合格的计数并丢弃。
- **一条可选的开放词汇 track。** SigLIP 2 视觉塔对常量文本原型打分，
  部署时用 `model.track: open_vocab` 选择。它不重训就能加类别，
  同一份图像嵌入既能用中文也能用英文回答，还能给出"这不在我的词表里"的
  分数。代价是慢约 4–5 倍。
- **不绑引脚的执行机构接口。** 运行时回调时带上一个类别；这个类别送到哪里
  是集成工作，正因如此同一份构建才能跑在排针不同的板子上。

## 适合接到哪里

- 家用与社区投放点：在投放的那一刻拍下单件物品，告诉居民该进哪个桶。
- 分拣工位：操作员逐件展示物品，需要一个第二意见外加一条 MQTT 上的审计记录。
- 带电动翻盖或分道指示灯的垃圾桶：由四分类结果经 GPIO 回调驱动。

不在范围内：传送带分拣的机械联动，以及路面散落垃圾检测——本模型假设一图一件。

## 实际效果如何

| 投放点能得到什么 | 典型值 | 设备 |
|---|---|---|
| 物品进画面到出投放答案 | 每次触发 **4.122 ms** | reComputer J40 系列（J4012，Orin NX） |
| 四分类 top-1 | **0.9500** | 同一模型，各加速器一致 |
| 物料 top-1（8 类） | **0.8877** | 同上 |
| 物料 top-1（1060 张子集），TensorRT 与 CPU 一致率 | **top-1 0.8755，一致率 0.9991** | reComputer J40 / J30 系列 |

**两个 top-1 要一起看**：四分类比物料高，是因为玻璃、金属、塑料之间的混淆
都被吸收进了"可回收物"——只报四分类会高估模型对材质的判别力。
Hailo-8 构建在全量 7417 张验证集上物料 top-1 为 0.8889。

### 平台支持

- **Jetson Orin（TensorRT）**——已在 reComputer J40 系列（Orin NX）上完成部署。
- **reComputer R2000（Hailo-8）**——已有部署包；基线 HEF 已在真实 Hailo-8 上
  跑完全量验证集。
- **RK3588 / RK3576**——转换与运行时在真机上跑通过，但没有部署包。

### 这些数字覆盖的范围

- **两个数据集都是单件物品的照片**，不是真实的垃圾桶——潮湿、压扁、堆叠、
  逆光与部分装袋的垃圾都在评测之外，真实投放点上的精度会低于这些数字，
  上线前应在自己现场的图像上复测。
- **`textile` 没有训练样本**，模型不会输出它；**`hazardous`（有害垃圾）
  没有任何物料类别映射过去**，本包模型不会发出它。
- **`organic` 在数据里占比约 48%**，模型会把拿不准的物品推向这一类。
- 四分类映射是本项目维护的一张表，各城市口径本就有差异。

## 分类器选型：基线 vs 开放词汇

两条 track 都出货，用 `model.track` 选择。

**基线（EfficientNet-Lite0，闭集）在这套分类法上更准**（val 0.8877 对 0.8501）。
**开放词汇（SigLIP 2）赢的是闭集结构上做不到的事：**

- **校准更好**——置信度更接近真实命中率，而一个阈值决定翻盖动不动时这有具体后果。
- **开放集拒识**——能给出"这件东西不在我的词表里"的分数（AUROC 0.7538）。
- **跨语言回答**——同一份视觉嵌入，中英文 prompt 在四分类上一致率 0.9143。
- **不重训就加类**——新类别是改 prompt 加重建原型，不是一次训练。

代价：CPU 上单图约 67 ms，是基线的 4–5 倍——要实时就得有 NPU/GPU。
另外两条部署结论：走"先物料八类、再映射四分类"的层级路径，不要直接预测
四分类（「可回收物」不是视觉概念）；`residual` 是兜底定义，不是视觉概念，
不要指望它能被"学会"。

## 输出接口

| 接口 | 位置 | 内容 |
|---|---|---|
| MQTT `waste/<stream-id>/results` | 端口 1883 | 一次分类一条 JSON：物料类别、中国四分类、置信度、top3、触发来源、图片引用、模型名与 ONNX sha256、分类法版本 |
| HTTP `/trigger` | 端口 8080 | POST 触发一次拍照分类 |
| HTTP `/preview.mjpg`、`/healthz`、`/events` | 端口 8080 | 实时画面、计数与推理耗时、最近结果与它们的 top3 |
| GPIO 回调 | 进程内 | 带四分类结果的异步回调。不绑引脚——那是集成工作。 |

图片永远不进 payload。`image_ref.kind` 取值 `none` / `local` /
`object_store`；payload 里出现 base64 图像字节属于违反契约，发布前会被拒。

## 部署方式对比

**摄像头 + reComputer J30 / J40（Orin）**——唯一随包带模型文件的套餐，也是
唯一提供开放词汇 track 的套餐。TensorRT engine 在部署时于设备上构建（绑定
GPU 架构与 TensorRT 版本），实测构建 68 秒，容器端到端 4.122 ms / 每次触发。

**摄像头 + reComputer R2000 系列（Hailo-8）**——验证三道 Hailo ABI 关卡后
下载 EfficientNet-Lite0 HEF。出货的 HEF 已在 Hailo-8 真机跑完验证全集 7417 张：
物料 top-1 0.8889、四分类 0.9507、p50 3.166 ms；部署容器本身也在真机上验证过
`/healthz`、`/trigger` 与 MQTT 触发链路。

## 使用须知

- **一图一件。** 没有检测器。一帧里两件物品只会得到一个答案，
  且它描述的是哪一件未定义。
- **相机与投放区就是全部输入。** 取景让物品在画面里过小会拉低分类效果。
- **连续模式下要连续三帧 top-1 相同才发布**，且该模式限速。
  触发模式没有这层平滑——一次拍摄就是一个答案。
- **随包的 MQTT broker 允许匿名连接（调试用）。** 正式部署换成带凭据的 broker。
- **GPIO 回调默认没接任何东西。** `actuator.enabled` 默认 false；
  不提供绑定代码就打开它不会有任何变化。

## 许可说明

上游代码与 SigLIP 2 checkpoint（`google/siglip2-base-patch16-224`）都是
Apache-2.0。两个训练数据集允许署名后再分发：TrashNet（MIT）与
Garbage Classification 3 — Material Identification / Roboflow Universe（CC BY 4.0）。
对外物料使用的署名字符串见 「gallery/ATTRIBUTION.md」。
