# Gallery attribution

## xiaozhi-stock-in.png (cover)

A photograph taken during a stock-in run: a SenseCAP Watcher in the hand,
screen showing the assistant's reply after a spoken stock-in command (5 units
added, 39 in total). Shot in-house; no third-party material.

## console-ovs-form-annotated.png

Screenshot of the speech-model dialog in the console, with the fields that have
to be filled in outlined. In-house UI.

## architecture.png

Diagram drawn for this package.

## Removed 2026-09-07

`wms-watcher-scenario.png` was the cover until 2026-09-07 and was **a truncated
PNG** — the file in git was incomplete and could not be decoded (Pillow: "image
file is truncated"). It was removed rather than replaced; the photograph above
took over as the cover.

## Not published here

`R1100_connected.png`, `watcher_usb.png`, `mcp-*.png`, `api-endpoint.png`,
`console-*.png`, the two GIFs and the two MP4s stay in this directory for the
guide but are not in `intro.gallery`. The terminal screenshots
(`mcp-bridge-success.png`, `mcp-endpoint.png`) show a session token in the
endpoint URL and should not be put on the page as they are.
