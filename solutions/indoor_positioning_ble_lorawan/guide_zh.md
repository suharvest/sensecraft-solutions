## 套餐: 入门套件 {#starter}

适合小型办公室或单个房间，面积 500 平方米以内。快速上手，硬件需求最少。

| 设备 | 数量 | 用途 |
|------|------|------|
| SenseCAP M2 网关 | 1 台 | LoRaWAN 网络覆盖 |
| BC01 蓝牙信标 | 6 个 | 定位参考点 |
| SenseCAP T1000 追踪器 | 1+ 个 | 被追踪的资产/人员 |

**部署完成后你可以：**
- 实时查看资产/人员位置
- 在网页地图上可视化展示
- 支持区域定位或三角定位

**覆盖范围：** 约 500 平方米 · LoRaWAN 覆盖半径 2 公里

## 步骤 1: 部署 BLE 信标 {#beacons type=manual required=true}

在室内各处安装 BLE 信标作为定位参考点。

### 接线

**根据定位精度选择安装数量**：
- 三角定位（米级精度）：每个区域至少 3 个信标
- 区域定位（判断在不在）：每个区域 1 个信标即可

**安装要求**：
- 安装高度：2.5-3 米（避免被人体遮挡）
- 信标间距：10-15 米
- 避免安装在金属表面附近

**安装步骤**：
1. 规划信标位置，画一张简单的布局图
2. 用双面胶或螺丝固定信标
3. 记录每个信标的 MAC 地址和安装位置（后面配置要用）

> MAC 地址印在信标背面，格式如 `AA:BB:CC:DD:EE:FF`

### 故障排查

| 问题 | 解决方法 |
|------|----------|
| 信标灯不亮 | 电池没电，更换 CR2477 电池 |
| 定位不准 | 信标太少或间距太大，增加信标密度 |
| 追踪器扫不到信标 | 信标安装太高或有遮挡，调整安装位置 |

---

## 步骤 2: 设置 LoRaWAN 网关 {#gateway type=manual required=true}

连接网关，建立追踪器与定位应用之间的通信。

### 接线

**安装步骤**：
1. 网关通电，连接网络（以太网或 WiFi）
2. 使用 SenseCraft App 扫码绑定网关
3. 等待 LED 常亮绿色，表示网关就绪

**放置建议**：
- 放在场地中央位置，覆盖范围最大
- 室内覆盖半径约 500 米，室外可达 2 公里
- 避免放在金属柜子里或地下室

### 故障排查

| 问题 | 解决方法 |
|------|----------|
| LED 不亮 | 电源问题，检查电源适配器和线缆 |
| LED 红色闪烁 | 网络未连接，检查网线或 WiFi 配置 |
| App 扫码失败 | 网关未联网，确保网关已连接互联网 |
| 追踪器数据不上报 | 频段不匹配，确认网关和追踪器使用相同频段（如 CN470） |

---

## 步骤 3: 部署定位应用程序 {#app_server type=docker_deploy required=true config=devices/app_deploy.yaml}

### 部署目标 {#app_server_local type=local config=devices/app_deploy.yaml default=true}

将室内定位应用程序部署到您的本地电脑。

### 接线

1. 确保 Docker Desktop 已安装并运行
2. 确保端口 5173 可用

### 部署完成

1. 访问 `http://localhost:5173`，使用 `admin` / `83EtWJUbGrPnQjdCqyKq` 登录
2. 上传楼层地图，在地图上标记信标位置（输入 MAC 地址）
3. 配置 LoRaWAN 网络服务器的 Webhook 指向 `http://你的IP:5173/api/webhook`

### 故障排查

| 问题 | 解决方法 |
|------|----------|
| 部署失败 | Docker 未运行，启动 Docker Desktop |
| 端口被占用 | 其他程序占用 5173，关闭占用程序或修改端口 |
| 网页打不开 | 服务未启动完成，等待几分钟后刷新页面 |

### 部署目标 {#app_server_remote type=remote config=devices/app_deploy.yaml}

通过 SSH 将室内定位应用程序部署到远程服务器。

### 接线

1. 将目标设备连接到网络
2. 获取设备 IP 地址
3. 获取 SSH 凭据（用户名/密码）
4. 确保远程服务器已安装 Docker

### 部署完成

1. 访问 `http://<设备IP>:5173`，使用 `admin` / `83EtWJUbGrPnQjdCqyKq` 登录
2. 上传楼层地图，在地图上标记信标位置（输入 MAC 地址）
3. 配置 LoRaWAN 网络服务器的 Webhook 指向 `http://<设备IP>:5173/api/webhook`

### 故障排查

| 问题 | 解决方法 |
|------|----------|
| SSH 连接失败 | IP 或凭据错误，检查 IP 地址和用户名密码 |
| 部署失败 | 远程服务器无 Docker，在远程服务器上安装 Docker |
| 网页打不开 | 防火墙阻止，开放远程服务器 5173 端口 |

---

## 步骤 4: 配置并激活追踪器 {#tracker type=manual required=true}

配置追踪器，在信标附近走动测试定位是否正常。

### 接线

1. 长按电源键 3 秒开机，绿灯闪烁表示正在入网
2. 入网成功后绿灯熄灭，追踪器进入工作状态
3. 打开 SenseCraft App，蓝牙连接追踪器
4. 设置工作模式为"BLE 扫描"
5. 选择正确的 LoRaWAN 区域（如 CN470、US915、EU868）
6. 保存配置
7. 走到信标附近，按追踪器按钮触发上报
8. 在定位应用网页上查看位置是否正确显示

### 故障排查

| 问题 | 解决方法 |
|------|----------|
| 开机后一直闪烁 | 入网失败，检查网关是否在线，频段是否匹配 |
| 网页上看不到追踪器 | Webhook 未配置，检查网络服务器的 Webhook 是否指向定位应用 |
| 位置不更新 | 追踪器休眠，按按钮触发上报，或调整上报间隔 |
| 位置显示错误 | 信标坐标配置错误，检查网页上信标的位置标记是否正确 |

---

## 步骤 5: 打开面板 {#dashboard type=web_dashboard required=true config=devices/dashboard.yaml}

定位面板已经运行。点击下方按钮在浏览器中打开。

### 故障排查
| 问题 | 解决方法 |
|------|----------|
| 页面无法加载 | 请确认前一个部署步骤已经成功，服务运行正常 |
| 主机/端口错误 | 如果你部署到远程设备，请用实际的设备 IP 更新地址 |

---
### 部署完成

室内定位系统已就绪！

#### 访问系统

- **访问地址：** http://\<服务器IP\>:5173
- **登录账号：** admin / 83EtWJUbGrPnQjdCqyKq

#### 初始设置

1. 上传楼层地图图片
2. 在地图上标记信标位置（输入 MAC 地址）
3. 配置 LoRaWAN 网络服务器的 Webhook

#### 快速验证

- 带着追踪器走到信标附近
- 按追踪器按钮触发位置上报
- 在网页看板上查看实时位置更新

#### 后续步骤

- [查看 Wiki 文档](https://wiki.seeedstudio.com/cn/solutions/indoor-positioning-bluetooth-lorawan-tracker/)
- [GitHub 仓库](https://github.com/Seeed-Solution/Solution_IndoorPositioning_H5)
- [体验在线演示](https://indoorpositioning-demo.seeed.cc/)

## 套餐: 标准配置 {#standard}

适合中型场所（500-2000 平方米），如仓库、办公楼、零售店等。

| 设备 | 数量 | 用途 |
|------|------|------|
| SenseCAP M2 网关 | 1 台 | LoRaWAN 网络覆盖 |
| BC01 蓝牙信标 | 15 个 | 定位参考点 |
| SenseCAP T1000 追踪器 | 3+ 个 | 被追踪的资产/人员 |

**部署完成后你可以：**
- 实时查看资产/人员位置
- 多区域覆盖，米级定位精度
- 网页地图可视化，支持历史轨迹回放

**覆盖范围：** 约 2000 平方米 · 支持多个房间/楼层

## 步骤 1: 部署 BLE 信标 {#beacons type=manual required=true}

在室内各处安装 BLE 信标作为定位参考点。

### 接线

**根据定位精度选择安装数量**：
- 三角定位（米级精度）：每个区域至少 3 个信标
- 区域定位（判断在不在）：每个区域 1 个信标即可

**安装要求**：
- 安装高度：2.5-3 米（避免被人体遮挡）
- 信标间距：10-15 米
- 避免安装在金属表面附近

**安装步骤**：
1. 规划信标位置，画一张简单的布局图
2. 用双面胶或螺丝固定信标
3. 记录每个信标的 MAC 地址和安装位置（后面配置要用）

> MAC 地址印在信标背面，格式如 `AA:BB:CC:DD:EE:FF`

### 故障排查

| 问题 | 解决方法 |
|------|----------|
| 信标灯不亮 | 电池没电，更换 CR2477 电池 |
| 定位不准 | 信标太少或间距太大，增加信标密度 |
| 追踪器扫不到信标 | 信标安装太高或有遮挡，调整安装位置 |

---

## 步骤 2: 设置 LoRaWAN 网关 {#gateway type=manual required=true}

连接网关，建立追踪器与定位应用之间的通信。

### 接线

**安装步骤**：
1. 网关通电，连接网络（以太网或 WiFi）
2. 使用 SenseCraft App 扫码绑定网关
3. 等待 LED 常亮绿色，表示网关就绪

**放置建议**：
- 放在场地中央位置，覆盖范围最大
- 室内覆盖半径约 500 米，室外可达 2 公里
- 避免放在金属柜子里或地下室

### 故障排查

| 问题 | 解决方法 |
|------|----------|
| LED 不亮 | 电源问题，检查电源适配器和线缆 |
| LED 红色闪烁 | 网络未连接，检查网线或 WiFi 配置 |
| App 扫码失败 | 网关未联网，确保网关已连接互联网 |
| 追踪器数据不上报 | 频段不匹配，确认网关和追踪器使用相同频段（如 CN470） |

---

## 步骤 3: 部署定位应用程序 {#app_server type=docker_deploy required=true config=devices/app_deploy.yaml}

### 部署目标 {#app_server_local type=local config=devices/app_deploy.yaml default=true}

将室内定位应用程序部署到您的本地电脑。

### 接线

1. 确保 Docker Desktop 已安装并运行
2. 确保端口 5173 可用

### 部署完成

1. 访问 `http://localhost:5173`，使用 `admin` / `83EtWJUbGrPnQjdCqyKq` 登录
2. 上传楼层地图，在地图上标记信标位置（输入 MAC 地址）
3. 配置 LoRaWAN 网络服务器的 Webhook 指向 `http://你的IP:5173/api/webhook`

### 故障排查

| 问题 | 解决方法 |
|------|----------|
| 部署失败 | Docker 未运行，启动 Docker Desktop |
| 端口被占用 | 其他程序占用 5173，关闭占用程序或修改端口 |
| 网页打不开 | 服务未启动完成，等待几分钟后刷新页面 |

### 部署目标 {#app_server_remote type=remote config=devices/app_deploy.yaml}

通过 SSH 将室内定位应用程序部署到远程服务器。

### 接线

1. 将目标设备连接到网络
2. 获取设备 IP 地址
3. 获取 SSH 凭据（用户名/密码）
4. 确保远程服务器已安装 Docker

### 部署完成

1. 访问 `http://<设备IP>:5173`，使用 `admin` / `83EtWJUbGrPnQjdCqyKq` 登录
2. 上传楼层地图，在地图上标记信标位置（输入 MAC 地址）
3. 配置 LoRaWAN 网络服务器的 Webhook 指向 `http://<设备IP>:5173/api/webhook`

### 故障排查

| 问题 | 解决方法 |
|------|----------|
| SSH 连接失败 | IP 或凭据错误，检查 IP 地址和用户名密码 |
| 部署失败 | 远程服务器无 Docker，在远程服务器上安装 Docker |
| 网页打不开 | 防火墙阻止，开放远程服务器 5173 端口 |

---

## 步骤 4: 配置并激活追踪器 {#tracker type=manual required=true}

配置追踪器，在信标附近走动测试定位是否正常。

### 接线

1. 长按电源键 3 秒开机，绿灯闪烁表示正在入网
2. 入网成功后绿灯熄灭，追踪器进入工作状态
3. 打开 SenseCraft App，蓝牙连接追踪器
4. 设置工作模式为"BLE 扫描"
5. 选择正确的 LoRaWAN 区域（如 CN470、US915、EU868）
6. 保存配置
7. 走到信标附近，按追踪器按钮触发上报
8. 在定位应用网页上查看位置是否正确显示

### 故障排查

| 问题 | 解决方法 |
|------|----------|
| 开机后一直闪烁 | 入网失败，检查网关是否在线，频段是否匹配 |
| 网页上看不到追踪器 | Webhook 未配置，检查网络服务器的 Webhook 是否指向定位应用 |
| 位置不更新 | 追踪器休眠，按按钮触发上报，或调整上报间隔 |
| 位置显示错误 | 信标坐标配置错误，检查网页上信标的位置标记是否正确 |

---

## 步骤 5: 打开面板 {#dashboard type=web_dashboard required=true config=devices/dashboard.yaml}

定位面板已经运行。点击下方按钮在浏览器中打开。

### 故障排查
| 问题 | 解决方法 |
|------|----------|
| 页面无法加载 | 请确认前一个部署步骤已经成功，服务运行正常 |
| 主机/端口错误 | 如果你部署到远程设备，请用实际的设备 IP 更新地址 |

---
### 部署完成

室内定位系统已就绪！

#### 访问系统

- **访问地址：** http://\<服务器IP\>:5173
- **登录账号：** admin / 83EtWJUbGrPnQjdCqyKq

#### 初始设置

1. 上传楼层地图图片
2. 在地图上标记信标位置（输入 MAC 地址）
3. 配置 LoRaWAN 网络服务器的 Webhook

#### 快速验证

- 带着追踪器走到信标附近
- 按追踪器按钮触发位置上报
- 在网页看板上查看实时位置更新

#### 后续步骤

- [查看 Wiki 文档](https://wiki.seeedstudio.com/cn/solutions/indoor-positioning-bluetooth-lorawan-tracker/)
- [GitHub 仓库](https://github.com/Seeed-Solution/Solution_IndoorPositioning_H5)
- [体验在线演示](https://indoorpositioning-demo.seeed.cc/)

## 套餐: 企业版 {#enterprise}

适合大型场所（2000 平方米以上），如工厂、医院、园区等。

| 设备 | 数量 | 用途 |
|------|------|------|
| SenseCAP M2 网关 | 1+ 台 | LoRaWAN 覆盖（面积大可增加） |
| BC01 蓝牙信标 | 30+ 个 | 定位参考点 |
| SenseCAP T1000 追踪器 | 10+ 个 | 被追踪的资产/人员 |

**部署完成后你可以：**
- 园区级实时定位追踪
- 高精度三角定位
- 可扩展到数千个追踪目标

**覆盖范围：** 2000+ 平方米 · 增加网关可覆盖多栋建筑

## 步骤 1: 部署 BLE 信标 {#beacons type=manual required=true}

在室内各处安装 BLE 信标作为定位参考点。

### 接线

**根据定位精度选择安装数量**：
- 三角定位（米级精度）：每个区域至少 3 个信标
- 区域定位（判断在不在）：每个区域 1 个信标即可

**安装要求**：
- 安装高度：2.5-3 米（避免被人体遮挡）
- 信标间距：10-15 米
- 避免安装在金属表面附近

**安装步骤**：
1. 规划信标位置，画一张简单的布局图
2. 用双面胶或螺丝固定信标
3. 记录每个信标的 MAC 地址和安装位置（后面配置要用）

> MAC 地址印在信标背面，格式如 `AA:BB:CC:DD:EE:FF`

### 故障排查

| 问题 | 解决方法 |
|------|----------|
| 信标灯不亮 | 电池没电，更换 CR2477 电池 |
| 定位不准 | 信标太少或间距太大，增加信标密度 |
| 追踪器扫不到信标 | 信标安装太高或有遮挡，调整安装位置 |

---

## 步骤 2: 设置 LoRaWAN 网关 {#gateway type=manual required=true}

连接网关，建立追踪器与定位应用之间的通信。

### 接线

**安装步骤**：
1. 网关通电，连接网络（以太网或 WiFi）
2. 使用 SenseCraft App 扫码绑定网关
3. 等待 LED 常亮绿色，表示网关就绪

**放置建议**：
- 放在场地中央位置，覆盖范围最大
- 室内覆盖半径约 500 米，室外可达 2 公里
- 避免放在金属柜子里或地下室

### 故障排查

| 问题 | 解决方法 |
|------|----------|
| LED 不亮 | 电源问题，检查电源适配器和线缆 |
| LED 红色闪烁 | 网络未连接，检查网线或 WiFi 配置 |
| App 扫码失败 | 网关未联网，确保网关已连接互联网 |
| 追踪器数据不上报 | 频段不匹配，确认网关和追踪器使用相同频段（如 CN470） |

---

## 步骤 3: 部署定位应用程序 {#app_server type=docker_deploy required=true config=devices/app_deploy.yaml}

### 部署目标 {#app_server_local type=local config=devices/app_deploy.yaml default=true}

将室内定位应用程序部署到您的本地电脑。

### 接线

1. 确保 Docker Desktop 已安装并运行
2. 确保端口 5173 可用

### 部署完成

1. 访问 `http://localhost:5173`，使用 `admin` / `83EtWJUbGrPnQjdCqyKq` 登录
2. 上传楼层地图，在地图上标记信标位置（输入 MAC 地址）
3. 配置 LoRaWAN 网络服务器的 Webhook 指向 `http://你的IP:5173/api/webhook`

### 故障排查

| 问题 | 解决方法 |
|------|----------|
| 部署失败 | Docker 未运行，启动 Docker Desktop |
| 端口被占用 | 其他程序占用 5173，关闭占用程序或修改端口 |
| 网页打不开 | 服务未启动完成，等待几分钟后刷新页面 |

### 部署目标 {#app_server_remote type=remote config=devices/app_deploy.yaml}

通过 SSH 将室内定位应用程序部署到远程服务器。

### 接线

1. 将目标设备连接到网络
2. 获取设备 IP 地址
3. 获取 SSH 凭据（用户名/密码）
4. 确保远程服务器已安装 Docker

### 部署完成

1. 访问 `http://<设备IP>:5173`，使用 `admin` / `83EtWJUbGrPnQjdCqyKq` 登录
2. 上传楼层地图，在地图上标记信标位置（输入 MAC 地址）
3. 配置 LoRaWAN 网络服务器的 Webhook 指向 `http://<设备IP>:5173/api/webhook`

### 故障排查

| 问题 | 解决方法 |
|------|----------|
| SSH 连接失败 | IP 或凭据错误，检查 IP 地址和用户名密码 |
| 部署失败 | 远程服务器无 Docker，在远程服务器上安装 Docker |
| 网页打不开 | 防火墙阻止，开放远程服务器 5173 端口 |

---

## 步骤 4: 配置并激活追踪器 {#tracker type=manual required=true}

配置追踪器，在信标附近走动测试定位是否正常。

### 接线

1. 长按电源键 3 秒开机，绿灯闪烁表示正在入网
2. 入网成功后绿灯熄灭，追踪器进入工作状态
3. 打开 SenseCraft App，蓝牙连接追踪器
4. 设置工作模式为"BLE 扫描"
5. 选择正确的 LoRaWAN 区域（如 CN470、US915、EU868）
6. 保存配置
7. 走到信标附近，按追踪器按钮触发上报
8. 在定位应用网页上查看位置是否正确显示

### 故障排查

| 问题 | 解决方法 |
|------|----------|
| 开机后一直闪烁 | 入网失败，检查网关是否在线，频段是否匹配 |
| 网页上看不到追踪器 | Webhook 未配置，检查网络服务器的 Webhook 是否指向定位应用 |
| 位置不更新 | 追踪器休眠，按按钮触发上报，或调整上报间隔 |
| 位置显示错误 | 信标坐标配置错误，检查网页上信标的位置标记是否正确 |

---

## 步骤 5: 打开面板 {#dashboard type=web_dashboard required=true config=devices/dashboard.yaml}

定位面板已经运行。点击下方按钮在浏览器中打开。

### 故障排查
| 问题 | 解决方法 |
|------|----------|
| 页面无法加载 | 请确认前一个部署步骤已经成功，服务运行正常 |
| 主机/端口错误 | 如果你部署到远程设备，请用实际的设备 IP 更新地址 |
### 部署完成

室内定位系统已就绪！

#### 访问系统

- **访问地址：** http://\<服务器IP\>:5173
- **登录账号：** admin / 83EtWJUbGrPnQjdCqyKq

#### 初始设置

1. 上传楼层地图图片
2. 在地图上标记信标位置（输入 MAC 地址）
3. 配置 LoRaWAN 网络服务器的 Webhook

#### 快速验证

- 带着追踪器走到信标附近
- 按追踪器按钮触发位置上报
- 在网页看板上查看实时位置更新

#### 后续步骤

- [查看 Wiki 文档](https://wiki.seeedstudio.com/cn/solutions/indoor-positioning-bluetooth-lorawan-tracker/)
- [GitHub 仓库](https://github.com/Seeed-Solution/Solution_IndoorPositioning_H5)
- [体验在线演示](https://indoorpositioning-demo.seeed.cc/)
