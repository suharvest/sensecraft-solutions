# Local broker for the fall-detection results.
#
# Anonymous on the LAN so the preview step and Home Assistant can subscribe
# without extra setup. Put it behind your own broker with credentials before
# exposing the device beyond a trusted network.
listener 1883 0.0.0.0
allow_anonymous true
persistence false
log_dest stdout
