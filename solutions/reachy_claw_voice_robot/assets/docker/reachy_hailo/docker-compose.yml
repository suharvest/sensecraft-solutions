# Reachy Mini Voice Robot — R2000 / Pi 5 + Hailo-8 deployment stack.
#
# Services:
#   docker compose up -d
#
# Data directory:
#   All persistent data (captures, face DB, etc.) is stored under DATA_DIR.
#   Default: ~/reachy-data   Override: DATA_DIR=/path/to/dir docker compose up -d

services:
  # ── Always-on services ────────────────────────────────────────────

  reachy-daemon:
    image: sensecraft-missionpack.seeed.cn/solution/reachy-daemon:v1.0
    container_name: reachy-daemon
    restart: unless-stopped
    network_mode: host
    privileged: true
    command: >-
      reachy-mini-daemon
      --serialport ${SERIAL_PORT:-auto}
      --localhost-only
      --deactivate-audio
      --fastapi-port 38001
    deploy:
      resources:
        limits:
          memory: 1G
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:38001/', timeout=2)"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  # Reachy Voice app (client-loop). R2000+Hailo preset: speech (SLV) + LLM run on
  # a REMOTE Jetson (set VOICE_ASSISTANT_HOST); vision runs locally on the Hailo-8
  # NPU (vision-hailo :8631, config.py's default). Drives the robot via :38001.
  reachy-voice:
    image: sensecraft-missionpack.seeed.cn/solution/reachy-voice:v0.5.0
    container_name: reachy-voice
    restart: unless-stopped
    network_mode: host
    privileged: true            # ALSA audio device + udev
    volumes:
      - /dev/shm:/dev/shm
      - /run/udev:/run/udev:ro
      - /var/run/docker.sock:/var/run/docker.sock
      - ${DATA_DIR:-~/reachy-data}/reachy-voice:/data
      - ./reachy-voice.yaml:/data/reachy-voice.yaml:ro
      - ./entrypoint.sh:/opt/reachy-voice/entrypoint.sh:ro
      - ./instructions.txt:/opt/reachy-voice/reachy_voice/profiles/exhibition/instructions.txt:ro
    environment:
      - NO_PROXY=localhost,127.0.0.1
      - REACHY_DAEMON_PORT=38001
      - REACHY_VOICE_CONFIG=/data/reachy-voice.yaml
      - REACHY_VOICE_DATA_DIR=/data
      - REACHY_LOG_LEVEL=INFO
      # Speech + LLM on the remote Jetson; vision is local (default 127.0.0.1:8631).
      - VOICE_ASSISTANT_HOST=${VOICE_ASSISTANT_HOST}
      - REACHY_V2V_URL=ws://${VOICE_ASSISTANT_HOST}:8621/v2v/stream
      # LLM_ENDPOINT (optional) overrides the LLM base URL; must be an
      # OpenAI-compatible base INCLUDING /v1. Empty → the bundled edge-llm.
      - REACHY_EDGE_LLM_URL=${LLM_ENDPOINT:-http://${VOICE_ASSISTANT_HOST}:11435/v1}
    depends_on:
      reachy-daemon:
        condition: service_healthy
    deploy:
      resources:
        limits:
          memory: 2G
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  # ── Vision (Hailo-8) ──────────────────────────────────────────────

  vision-hailo:
    image: sensecraft-missionpack.seeed.cn/solution/vision-hailo:v0.3
    container_name: vision-hailo
    restart: unless-stopped
    network_mode: host
    devices:
      - /dev/hailo0:/dev/hailo0
      - ${CAMERA_DEVICE:-/dev/video0}:${CAMERA_DEVICE:-/dev/video0}
    volumes:
      - ${DATA_DIR:-~/reachy-data}/vision:/var/lib/vision-hailo
      - /run/udev:/run/udev:ro
      - /usr/lib/libhailort.so.4.21.0:/usr/lib/libhailort.so.4.21.0:ro
      - /usr/lib/libhailort.so:/usr/lib/libhailort.so:ro
    environment:
      - ZMQ_PUB_PORT=8631
      - HTTP_PORT=8630
      - CAMERA_DEVICE=${CAMERA_DEVICE:-/dev/video0}
      - HEF_DETECT=/app/models/scrfd_2.5g.hef
      - HEF_EMOTION=/app/models/hsemotion_b0.hef
      - CAPTURE_DIR=/var/lib/vision-hailo/captures
      - FACE_DB_DIR=/var/lib/vision-hailo/faces
      - REST_CTRL_URL=tcp://reachy:18791
    deploy:
      resources:
        limits:
          memory: 2G
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8630/', timeout=2)"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
