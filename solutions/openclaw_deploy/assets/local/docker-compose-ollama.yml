services:
  ollama:
    image: ollama/ollama
    container_name: ollama
    restart: unless-stopped
    network_mode: host
    volumes:
      - ollama-data:/root/.ollama
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  ollama-data:
