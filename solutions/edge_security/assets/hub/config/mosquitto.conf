# Broker for the detection layer. Anonymous on the LAN — put the hub host on a
# management VLAN, or add a password_file here before exposing 1883 wider.
listener 1883
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
log_dest stdout
max_packet_size 262144
