<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1100 560" width="1100" height="560" font-family="Inter, Helvetica, Arial, sans-serif">
  <rect width="1100" height="560" fill="#ffffff"/>
  <defs>
    <marker id="a2" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
      <path d="M0 0 L10 5 L0 10 z" fill="#2f6f8f"/>
    </marker>
  </defs>
  <text x="40" y="50" fill="#12232e" font-size="26" font-weight="700">One machine per site — broker, hub and detector together</text>
  <text x="40" y="78" fill="#5c7280" font-size="16">Each preset is self-contained. Nothing below requires a second machine.</text>

  <rect x="40" y="104" width="500" height="230" rx="12" fill="#f2f7fa" stroke="#c3d6e0"/>
  <text x="62" y="136" fill="#12232e" font-size="19" font-weight="700">Preset A — Jetson Single Box</text>
  <text x="62" y="164" fill="#42606f" font-size="16">camera :554 → CPU/ONNX detector → mosquitto :1883 → hub</text>
  <text x="62" y="190" fill="#42606f" font-size="16">all three containers on the same Jetson</text>
  <rect x="62" y="208" width="456" height="46" rx="8" fill="#ffffff" stroke="#c3d6e0"/>
  <text x="80" y="237" fill="#2f6f8f" font-size="15">detector · mosquitto · hub — workbench served here on :8090</text>
  <text x="62" y="284" fill="#7d94a1" font-size="15">no TensorRT path yet, so the GPU is idle</text>
  <text x="62" y="308" fill="#7d94a1" font-size="15">30-37 ms inference, 14.92 fps against a 15 fps source</text>

  <rect x="560" y="104" width="500" height="230" rx="12" fill="#eef7f3" stroke="#a9d6c4"/>
  <text x="582" y="136" fill="#12232e" font-size="19" font-weight="700">Preset B — RK3588 Single Box</text>
  <text x="582" y="164" fill="#42606f" font-size="16">camera :554 → NPU detector (hw decode) → mosquitto → hub</text>
  <text x="582" y="190" fill="#42606f" font-size="16">all three containers on the same RK3588 board</text>
  <rect x="582" y="208" width="456" height="46" rx="8" fill="#ffffff" stroke="#a9d6c4"/>
  <text x="600" y="237" fill="#2f6f8f" font-size="15">detector · mosquitto · hub — workbench served here on :8090</text>
  <text x="582" y="284" fill="#7d94a1" font-size="15">41.9 ms p50 inference, NPU 8%, 21% of one CPU core</text>
  <text x="582" y="308" fill="#7d94a1" font-size="15">the hub costs 1.4% of one core and 44.9 MB alongside it</text>

  <rect x="40" y="364" width="1020" height="126" rx="12" fill="#ffffff" stroke="#c3d6e0" stroke-dasharray="7 5"/>
  <text x="62" y="396" fill="#5c7280" font-size="19" font-weight="700">Optional expansion — Shared Hub</text>
  <text x="62" y="424" fill="#7d94a1" font-size="16">Only when several detector boxes already run and you want one alert list across them: repoint each detector's</text>
  <text x="62" y="448" fill="#7d94a1" font-size="16">mqtt_host at a separate always-on machine running broker + hub. Reserved for later: reCamera and Hailo detectors —</text>
  <text x="62" y="472" fill="#7d94a1" font-size="16">the payload contract is published, so any device that emits it joins unchanged. Neither is built yet.</text>

  <line x1="550" y1="364" x2="550" y2="340" stroke="#2f6f8f" stroke-width="2" marker-end="url(#a2)"/>
  <text x="565" y="356" fill="#2f6f8f" font-size="15">optional: detections + status over MQTT to a shared hub</text>

  <text x="40" y="524" fill="#42606f" font-size="16">Topics: sensecraft/security/&lt;device_id&gt;/detections/&lt;stream_id&gt; · /status · /snapshot/&lt;event_id&gt; · /events/&lt;stream_id&gt;</text>
  <text x="40" y="548" fill="#7d94a1" font-size="15">The hub judges rules only for detectors that carry track ids. Devices that judge on their own publish events directly.</text>
</svg>
